<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BasicCalculator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimaryDark">@color/orange</item>
    </style>

    <style name="Theme.BasicCalculator" parent="Base.Theme.BasicCalculator" />

    <style name="OperatorButton" parent="CommonButton">
        <item name="android:textColor">@color/orange</item>
    </style>

    <style name="DigitButton" parent="CommonButton">

        <item name="android:textColor">@color/black</item>
    </style>
    
    <style name="CommonButton" parent="Widget.MaterialComponents.ExtendedFloatingActionButton">
        <item name="android:layout_width">68dp</item>
        <item name="android:layout_height">68dp</item>
        <item name="cornerRadius">36dp</item>
        <item name="backgroundTint">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">32dp</item>
        <item name="android:layout_margin">12dp</item>
    </style>
</resources>